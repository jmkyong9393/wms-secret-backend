apiVersion: v1
kind: Service
metadata:
  name: wms-api-service
  namespace: wms
  labels:
    app: wms-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: wms-api
