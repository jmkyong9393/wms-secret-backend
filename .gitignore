# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# IDEs
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Logs
# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# IDEs
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Logs
logs/
*.log
# 테스트 실행 로그는 그 시점 코드의 검증 증빙이므로 추적한다
!tests/results/*.log

# pytest
.pytest_cache/
.coverage
htmlcov/

# Archive Backups
archive/

# AI Training Runs & Large Weights (>100MB)
runs/
*.pt
*.onnx
*.engine

# --- 시크릿 및 로컬 전용 산출물 (2026-08-04 추가) ---
# 실제 AWS 액세스 키가 평문으로 들어있는 1회성 스크래치 스크립트. 절대 커밋 금지.
2.env
*.env
scratch_*.py
# 검수 원본 이미지. S3(CloudFront)가 정본이며 로컬 사본은 YOLO 추론 캐시일 뿐이라 커밋 대상이 아니다.
app/experiment_data/
# RAG 벡터 DB 빌드 산출물
ai_knowledge_base/chroma_db/

# Claude
.claude
# VLM 크롭 디버그 산출물 (WMS_SAVE_VLM_CROPS)
app/ai/_vlm_crops/

# 프린트 브리지 로컬 설정 (백엔드 계정 비밀번호 포함 - 커밋 금지)
scripts/bridge_config.json

# 라이브 DB 읽기전용 조회용 접속 문자열 (비밀번호 포함 - 커밋 금지)
.env.readonly

# PyInstaller 빌드 산출물 (print_bridge.exe 등 - 20MB, 커밋 금지)
scripts/_build/
scripts/_dist/
